# Netflix Format Checker - Requirements

# Web Framework
Flask==3.0.0
Werkzeug==3.0.1

# HTTP Requests
requests==2.31.0

# Cryptography (for MSL encryption)
pycryptodomex==3.19.0

# Optional: Better logging
colorlog==6.8.0

# Poster fetching from TMDB (The Movie Database)
# Note: Uses free TMDB API - no authentication key required for basic searches
# Register for your own key at: https://www.themoviedb.org/settings/api
